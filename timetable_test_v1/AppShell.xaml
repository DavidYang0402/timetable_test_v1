<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="timetable_test_v1.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view ="clr-namespace:timetable_test_v1.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate view:LoginPage}"
        Route="Login" />

    <TabBar>
        <ShellContent
            Title="Home"
            Icon="home.png"
            ContentTemplate="{DataTemplate view:HomePage}"
            Route="Home" />

        <ShellContent
            Title="Search"
            ContentTemplate="{DataTemplate view:SearchPage}"
            Route="Search" 
            Icon="Search"/>

        <ShellContent
            Title="User"
            ContentTemplate="{DataTemplate view:UserInformationPage}"
            Route="UserInfo" 
            Icon="user.png"/>
    </TabBar>
</Shell>
    